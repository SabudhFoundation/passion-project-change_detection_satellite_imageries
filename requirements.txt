# Python 3.10–3.12 recommended (3.11 tested)
# Course / pip users: pip install -r requirements.txt
# Poetry users: prefer pyproject.toml — poetry lock && poetry install
# Install GDAL system libraries before rasterio on Linux: apt install gdal-bin libgdal-dev

tensorflow>=2.15.0,<2.18.0
numpy>=1.23.0,<2.0.0
matplotlib>=3.7.0
rasterio>=1.3.0
Pillow>=9.0.0
scikit-learn>=1.2.0
tqdm>=4.65.0
python-dotenv>=0.5.1

# Development / reporting (from course template)
click
coverage
flake8
