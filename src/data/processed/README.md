# Processed outputs

Training and inference write artifacts here by default (`UCDNET_OUTPUT_DIR`):

- `artifacts/best_model.keras` — best checkpoint
- `artifacts/metrics.csv` — per-epoch log
- `artifacts/training_curves.png` — loss / F1 plots
- `artifacts/predictions/` — inference outputs

These files are gitignored.
