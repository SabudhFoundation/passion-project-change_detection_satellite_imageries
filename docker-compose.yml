services:
  ucdnet-cpu:
    build: .
    image: change-detection:latest
    profiles: ["cpu"]
    volumes:
      - ./data/raw/onera-satellite-change-detection-dataset:/data/oscd:ro
      - ./data/processed/artifacts:/app/data/processed/artifacts
    environment:
      UCDNET_DATA_ROOT: /data/oscd
      UCDNET_OUTPUT_DIR: /app/data/processed/artifacts
    command: ["train", "--epochs", "30"]

  ucdnet-gpu:
    build: .
    image: change-detection:latest
    profiles: ["gpu"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ./data/raw/onera-satellite-change-detection-dataset:/data/oscd:ro
      - ./data/processed/artifacts:/app/data/processed/artifacts
    environment:
      UCDNET_DATA_ROOT: /data/oscd
      UCDNET_OUTPUT_DIR: /app/data/processed/artifacts
      NVIDIA_VISIBLE_DEVICES: all
    command: ["train", "--epochs", "30"]

  streamlit:
    build: .
    image: change-detection:latest
    profiles: ["streamlit"]
    ports:
      - "8501:8501"
    volumes:
      - ./data/processed/artifacts:/app/data/processed/artifacts
      - ./data/raw/onera-satellite-change-detection-dataset:/data/oscd:ro
    environment:
      UCDNET_DATA_ROOT: /data/oscd
      UCDNET_OUTPUT_DIR: /app/data/processed/artifacts
    entrypoint: ["poetry", "run", "streamlit"]
    command: ["run", "streamlit_app/app.py", "--server.port=8501", "--server.address=0.0.0.0"]

  streamlit-gpu:
    build: .
    image: change-detection:latest
    profiles: ["streamlit-gpu"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    ports:
      - "8501:8501"
    volumes:
      - ./data/processed/artifacts:/app/data/processed/artifacts
      - ./data/raw/onera-satellite-change-detection-dataset:/data/oscd:ro
    environment:
      UCDNET_DATA_ROOT: /data/oscd
      UCDNET_OUTPUT_DIR: /app/data/processed/artifacts
    entrypoint: ["poetry", "run", "streamlit"]
    command: ["run", "streamlit_app/app.py", "--server.port=8501", "--server.address=0.0.0.0"]

  predict:
    build: .
    image: change-detection:latest
    profiles: ["predict"]
    volumes:
      - ./data/processed/artifacts:/app/data/processed/artifacts
      - ./input:/input:ro
    environment:
      UCDNET_OUTPUT_DIR: /app/data/processed/artifacts
    entrypoint: ["poetry", "run", "ucdnet", "predict"]
    command:
      - "--model"
      - "/app/data/processed/artifacts/best_model.keras"
      - "--t1"
      - "/input/t1"
      - "--t2"
      - "/input/t2"
      - "--out"
      - "/app/data/processed/artifacts/predictions/change_map.tif"
