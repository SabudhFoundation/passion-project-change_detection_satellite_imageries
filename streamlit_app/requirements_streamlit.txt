# Extra dependencies for the Streamlit UI
# Install on top of your existing requirements.txt:
#   pip install -r streamlit_app/requirements_streamlit.txt

streamlit>=1.35
plotly>=5.0
pandas>=2.0
matplotlib>=3.7
Pillow>=10.0

# Optional — interactive map viewer
folium>=0.16
streamlit-folium>=0.20

# Already in requirements.txt but listed for clarity:
# tensorflow>=2.15
# numpy
# rasterio
# tqdm
